---
description: Notion 需求管理规范。当涉及到查看需求、更新需求状态、读写 Notion 文档时使用此规则
globs:
alwaysApply: true
---

# Notion 需求管理规范

## MCP 服务选择

- **必须使用 `notionApiPDDAutoReply`** 这个 MCP 服务访问项目需求文档
- 禁止使用 `notionApi`（该服务连接的是"尾巴舞动"工作区，无法访问本项目的 Notion 文档）

## 需求数据库

| 配置项 | 值 |
|--------|-----|
| 数据库名称 | 多米软件操作界面 |
| Database ID | `2c574ef65e5a8084b39ffef9fdf2a853` |
| Data Source ID | `2c574ef65e5a80949a1a000bfa42d559` |

## 数据库字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 界面名称 | title | 需求/界面名称 |
| 功能分类 | select | 店铺、客户会话、智能问答匹配 |
| 状态 | status | 已完成、进行中、待记录 |
| 操作路径 | rich_text | 功能的导航路径 |
| 主要操作按钮 | rich_text | 界面包含的操作按钮 |
| 界面描述 | rich_text | 界面的详细功能描述 |
| 界面截图 | files | 界面参考截图 |
| 更新日期 | date | 最后更新时间 |

## 查询示例

### 查询所有需求
```
CallMcpTool: user-notionApiPDDAutoReply
toolName: API-query-data-source
arguments: { "data_source_id": "2c574ef65e5a80949a1a000bfa42d559", "page_size": 100 }
```

### 按状态筛选
```
arguments: {
  "data_source_id": "2c574ef65e5a80949a1a000bfa42d559",
  "filter": {
    "property": "状态",
    "status": { "equals": "进行中" }
  }
}
```

### 按功能分类筛选
```
arguments: {
  "data_source_id": "2c574ef65e5a80949a1a000bfa42d559",
  "filter": {
    "property": "功能分类",
    "select": { "equals": "客户会话" }
  }
}
```

## 更新需求状态

开发完成某个功能后，应更新对应需求的状态：

```
CallMcpTool: user-notionApiPDDAutoReply
toolName: API-patch-page
arguments: {
  "page_id": "<page_id>",
  "properties": {
    "状态": { "status": { "name": "已完成" } },
    "更新日期": { "date": { "start": "<today>" } }
  }
}
```
