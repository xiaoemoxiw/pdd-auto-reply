---
description: 拼多多网页注入脚本开发规范。当涉及到修改注入脚本、DOM 选择器、消息监听、自动回复逻辑时使用此规则
globs: src/inject/**
---

# 拼多多网页注入脚本规范

## 概述

注入脚本运行在拼多多官方网页的上下文中，通过 `executeJavaScript` 注入到 BrowserView。
脚本负责：监听新消息 DOM 变化、模拟输入和发送消息、提供调试接口。

## DOM 选择器管理

### 选择器统一管理
所有 DOM 选择器必须集中定义在 `SELECTORS` 对象中：

```js
const SELECTORS = {
  chatContainer: '...',     // 聊天消息容器
  customerMessage: '...',   // 买家消息元素
  messageText: '...',       // 消息文本内容
  inputBox: '...',          // 输入框
  sendButton: '...',        // 发送按钮
  customerName: '...',      // 当前客户名称
  sessionItem: '...'        // 会话列表项
};
```

### 选择器编写原则
- 每个选择器提供多个候选值（逗号分隔），按优先级排列
- 优先使用语义化的 class 名，其次使用 `[class*="xxx"]` 模糊匹配
- 拼多多页面可能使用 CSS Modules（带 hash 后缀），需要用属性选择器模糊匹配
- 选择器需要在实际登录后通过 DevTools 验证和调整

### 调试接口
必须暴露 `window.__PDD_HELPER__` 对象，至少包含：
- `getSelectors()` — 查看当前选择器
- `setSelector(key, value)` — 运行时修改选择器
- `sendMessage(text)` — 手动触发发送消息
- `isEnabled()` — 查看自动回复开关状态

## 消息监听

### 使用 MutationObserver
- 监听聊天容器的 `childList` 变化（`subtree: true`）
- 容器未找到时每 2 秒重试（页面可能还在加载）
- 必须去重（记录 `lastMessageText` 避免重复触发）

### 消息转发
- 新消息通过 `window.postMessage` 发送给 pdd-preload.js
- 消息格式固定为：`{ type: 'PDD_NEW_MESSAGE', message, customer, conversationId }`

## 模拟发送消息

### 输入框填写
- 区分 `textarea/input` 和 `contenteditable` 两种输入框类型
- 对 `textarea/input` 使用 `nativeInputValueSetter` 绕过 React 受控组件
- 填写后必须触发 `input` 和 `change` 事件

### 触发发送
- 优先查找发送按钮并 `click()`
- 按钮未找到时尝试 Enter 键模拟
- 输入和发送之间留 300ms 延迟（等待 UI 响应）

## 安全约束
- 注入脚本**不能使用 Node.js API**（运行在网页上下文）
- 使用 IIFE 包裹避免污染全局作用域
- 使用 `__PDD_AUTO_REPLY_INJECTED__` 标志防止重复注入
