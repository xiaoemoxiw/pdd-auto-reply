---
description: 多人协作、共享入口与最小结构演进的统一规则
globs:
alwaysApply: true
---

# 多人协作与共享入口规则

## 分工方式
- 优先按文件边界和模块边界分工，不只按页面名称或业务名称口头分工。
- 共享接线层与业务实现层分开维护；需要多人并行时，先确认谁负责共享入口收口。

## 共享接线层
- `src/renderer/index.html`、`src/main/main.js`、`src/preload/preload.js` 默认视为共享接线层。
- 共享接线层只处理导航注册、视图切换、容器挂载、preload 暴露、IPC 注册和生命周期编排。
- 除非当前需求必须如此，否则不要把完整业务逻辑、大段页面结构或复杂状态管理继续堆入共享接线层。

## 新模块落点
- 新增业务页、管理页、面板或长逻辑时，优先新建独立文件、独立脚本或独立视图容器承载。
- 新模块若暂时只有骨架，也应优先提供稳定入口和独立落点，方便后续多人并行扩展。
- 结构调整应顺着仓库现有模式做最小演进，不为了协作单独制造一套新的架构层。

## 改动原则
- 如果某项结构改动只是为减少冲突，但不会提升清晰度、维护性或职责边界，就不要做。
- 修改共享入口时，只做当前需求所需的最小接线，不顺手搬动无关模块。
- 交付前至少确认已有主流程未被共享入口改动破坏，尤其是聊天、店铺切换和既有 IPC。
