---
description: 当任务涉及需求查询、需求状态同步、Notion 文档读取或研发记录维护时使用此规则
globs:
alwaysApply: false
---

# Notion 工作流规则

## 使用前提
- 只有在当前环境确实接入了可访问本项目工作区的 Notion MCP 服务时，才把 Notion 作为工作流的一部分。
- 如果当前环境只有通用 Notion 服务，且无法确认它连接的是本项目工作区，不要把 Notion 当作强依赖或唯一事实来源。
- 规则冲突时，以用户最新要求和仓库真实实现为准，不用过时的 Notion 页面覆盖当前代码现实。

## 服务选择
- 读写 Notion 前，先确认当前环境里实际可用的 MCP 服务名和它对应的工作区。
- 如果没有项目专用服务，就只把 Notion 当补充参考，不主动修改需求状态或结构化页面内容。
- 不要沿用旧 Cursor 规则里已经不存在的固定服务名。

## 使用原则
- 查询需求时，优先用于补充产品背景、字段含义、流程约束，不替代代码事实。
- 更新状态、写评论或改页面前，先确认目标页面、字段和当前工作区权限都正确。
- 如果 Figma、代码实现、Notion 三者不一致，优先记录差异，不直接用其中一方强行覆盖另外两方。

## 写入约束
- 更新需求状态时，只修改当前任务明确涉及的页面或字段，不批量改动无关内容。
- 写入前先读取当前页面或数据源结构，确认字段类型与当前数据库配置一致。
- 不在 Notion 中写入敏感凭据、Token、Cookie、本地路径或调试日志原文。

## 输出要求
- 如果任务依赖了 Notion，需要说明它扮演的是主依据还是补充参考。
- 如果因为 MCP 服务或权限原因无法使用 Notion，需要明确说明限制，不臆测页面内容。
