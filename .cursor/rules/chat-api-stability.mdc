---
description: 当任务涉及 chat-api 链路（接口对接的客户对话）会话列表、消息拉取、商品卡片、轮询初始化或脱敏抓包数据时使用此规则
globs:
alwaysApply: false
---

# chat-api 链路稳定性规则

本规则收口 `docs/chat-api-stability-handover.md` 与 `docs/chat-api-recovery-handover.md` 中"不要轻易回退"的设计决策。修改 `chat-api`（"客户对话（接口对接）"）相关代码前必须遵循。

## 脱敏抓包数据隔离

- `getApiTraffic(shopId)` 仅返回运行时实抓的请求/响应；**禁止**回灌 `getPersistedApiTraffic` / `apiTrafficLogPath` 这类持久化脱敏副本。
- 持久化抓包文件（`artifacts/api-traffic/*`）已经把 `content/nickname` 脱敏为 `[TEXT]`、`uid/session_id` 脱敏为 `[ID]`、`anti_content/cookie` 脱敏为 `[REDACTED]`、URL 脱敏为 `[URL]`、请求 ID 脱敏为 `[REQUEST_ID]`。这五个占位符既不能当请求模板（发出去服务器 500），也不能当渲染兜底（会话列表会出现 `[TEXT]` 假数据）。
- 任何向 `setApiSessionSnapshot` / `enqueueRendererApiSessionUpdate` 喂会话条目的新路径，必须先经过 `isSanitizedApiSessionEntry()` 过滤；过滤覆盖字段包括 `sessionId / customerId / userUid / customerName / customerAvatar / lastMessage / conversationId / chatId / rawId`。
- 修复"脱敏数据回流"问题时，先检查是不是新代码路径直接读了 `getPersistedApiTraffic` 或 `apiTrafficLogPath`，再考虑加白名单/转换；不要为了"启动就有兜底数据"绕开过滤防线。

## 会话初始化与轮询边界

- 后台轮询路径（`_pollMessagesForSession` / `_doPoll`）**全程** `allowInitSession: false`。一旦放开，会立即重新引发后台 `chat-merchant` BrowserView 互踢、整店掉线。
- 用户主动入口（`loadShopApiSessions`、`api-get-messages` 等 IPC）保持 `allowInitSession: true`，依赖 `PddApiClient` 内部 `_sessionInited` sticky 标志去重；**不要**写死 `false`，否则 fallback 路径在没模板时会直接 `return []`。
- `getSessionList` 不再走"前置 init"模式：先直接发请求；只有"直调失败 + 缓存为空 + 允许 init + 尚未 init"时才触发一次 `initSession()` 重试。
- `getSessionMessages` 同样不前置 init；polling 路径的 safe-mode 短路（`!allowInit && !template && !anti`）只允许出现在 polling 调用方传入的场景中。
- `initSession` 等待循环必须采用双判定提前 break：URL 跳到 `chat-merchant` ⇒ 就绪；`_getConversationBootstrapStatus().ready` ⇒ 就绪；URL 跳到 `/login` ⇒ 立即终止并触发 `authExpired`。上限 10s（`20 × 500ms`），不要回到只判 URL 的 20s 死等。

## 店铺在线校验

- `getApiSessionsByScope` 在目标店铺为空且 `shopManager.safeValidateShops` 可用时，自动跑一次批量接口校验把未在线店铺升 online。这个自动校验**仅在未在线时触发**，已经在线的店铺不会重复跑。
- 不要把 `safeValidateShops` 兜底扩散到所有 IPC 入口（如 polling、消息拉取），避免每个调用都拖一次校验耗时。
- 校验失败仍按原逻辑抛错，不要在外层吞掉错误掩盖 token 真过期的情况。

## 商品卡片 fallback

- `buildApiGoodsCardFallback` 必须**先**在 `[raw.info.goods_info, raw.info.data.goods_info, extra.goods_info, raw.extra.goods_info, raw.biz_context.goods_info, raw.goods_info, session.goodsInfo, session.raw.goods_info, session.raw.goods]` 里找第一个对象，然后**精确按字段名取值**：`goods_id` / `goods_name` / `goods_thumb_url` / `mall_link_url` / `min_group_price` / `group_price` / `promotion_price` / `min_normal_price` / `min_price` / `price`。
- 旧的 `pickApiGoodsText` / `pickApiGoodsNumber` 深度遍历仅作兜底，必须保持以下约束：
  - `title` 的 keys 不允许包含独立的 `'title'`（避免命中 `raw.info.title = "当前用户来自 商品详情页"` 这类系统提示文案）。
  - `priceText` 的 keys 不允许包含独立的 `'price'`（避免深度遍历命中 `goods_id` 数字后被 `formatApiGoodsPrice` 的 `/100` 规则放大成离谱价）。
  - `imageUrl` 的 keys 把 `goods_thumb_url` 排在最前。
- 商品来源通知卡片 `priceText` 经常为空属于正常现象（PDD 后台在该消息里没下发价格字段）；不要为了"显示价格"用 `total_amount` 等不确定字段硬填。

## 自动副作用收口

- 进入 `chat-api` 后**不**自动恢复上次会话、**不**自动刷新当前会话详情、**不**自动 `apiMarkLatestConversations()`、**不**自动 `loadApiTokenStatus()`、**不**因 `chat/list` 失败自动向全局抛 `authExpired`。
- 侧栏订单/退款（`apiGetSideOrders` / `apiGetRefundOrders`）不允许默认自动加载；要加载必须由用户显式触发，且调用时显式带 `allowPageRequest: false`，避免 `refund-order:page-request` 二次拉起 `chat-merchant`。
- "嵌入网页聊天"入口已停用，左侧任何 `chat` 相关导航必须统一跳转到 `chat-api`，不要再恢复嵌入聊天页作为默认。

## 验证最低要求

修改 `chat-api` 链路相关代码后，至少完整跑一遍以下三段（沿用 `docs/chat-api-recovery-handover.md`）：

1. **启动 → 列表**：完全退出后 `npm run dev`，直接进入「客户对话（接口对接）」，确认日志出现 `[API] 拉取会话列表 { templateSource: 'fallback' }` + `latest_conversations -> 200` + `api-get-sessions 返回 N 条`，列表显示真实昵称/头像/最后消息，**没有** `[TEXT]` / `[ID]` / `[REDACTED]` / `[URL]` / `[REQUEST_ID]` 占位符。
2. **点击会话 → 消息**：点列表任一会话，确认 `POST .../plateau/chat/list -> 200`，中间面板显示消息，商品来源通知卡片显示真实标题与主图。
3. **静置 → 不掉线**：静置 5~10 分钟，确认日志**不再出现** `/service-market/chat/order-list`、`/chats/getToken`、`wss://m-ws.pinduoduo.com`、`wss://titan-ws.pinduoduo.com`、`/janus/api/heartbeat/v2`、`refund-order:page-request 触发聊天页准备`。

## 异常排查指引

- 列表/消息出现 `[TEXT]` / `[ID]` 占位符 ⇒ 检查是不是新代码路径直接读了 `getPersistedApiTraffic` 或 `apiTrafficLogPath`，必要时在塞 snapshot 之前补 `isSanitizedApiSessionEntry()` 过滤。
- 列表加载又变 ~20s ⇒ 检查 `_pollMessagesForSession` / `_doPoll` / `loadShopApiSessions` 是否被改回 `allowInitSession: false`，并核对 `_getConversationBootstrapStatus().ready` 双判定逻辑是否被破坏。
- 后台聊天运行时被带起 ⇒ 优先查近期是否新增了未带 `allowPageRequest: false` 的 `requestViaPddPage()` / `ensurePddPageViewReady()` 调用，或新增的视图初始化是否回到了 `chat-merchant`。

## 不要轻易回退的清单（强约束）

- `getApiTraffic` 不再回灌持久化脱敏 traffic。
- `isSanitizedApiSessionEntry` 占位符过滤防线常驻。
- 后台 polling 全程 `allowInitSession: false`。
- `loadShopApiSessions` / `api-get-messages` 保持 `allowInitSession: true`，依赖 sticky 标志。
- `getApiSessionsByScope` 自动 `safeValidateShops` 仅在未在线时触发。
- 自动回复保持"接口优先 + DOM 兜底"，不能让 `network-monitor` / `embedded-dom` 来源退回 DOM 发送。
- `apiGetRefundOrders` / `apiGetSideOrders` 在 `chat-api` 场景下默认安全模式或显式 `allowPageRequest: false`。

## 关键文件

### 主进程（PddApiClient 已模块化）

- `src/main/pdd-api.js`：PddApiClient facade，公共 IPC 表面与状态标志
- `src/main/pdd-business-api-client.js`：共享请求基座（cookie / 重试 / 登录页识别 / 错误归一化）
- `src/main/pdd-api/modules/session-init-module.js`：`initSession()` 双判定就绪 + auth 失效兜底
- `src/main/pdd-api/modules/chat-sessions-module.js`：`getSessionList` 多级回退、按订单号查找、`sessionCache` 持有方
- `src/main/pdd-api/modules/chat-polling-module.js`：`_doPoll` / `_pollMessagesForSession` / `markLatestConversations`、`allowInitSession: false` 安全模式约束
- `src/main/pdd-api/modules/message-send-module.js`：自动回复 / 手动发送的接口优先链路
- `src/main/pdd-api/modules/shop-profile-module.js`：店铺核身 + `probeCommonMallInfoRequest` 安全模式探针
- `src/main/pdd-api/modules/goods-card-module.js`：商品卡片接口兜底
- `src/main/pdd-api/parsers/session-parsers.js` / `message-parsers.js` / `goods-parsers.js`：纯解析层（占位符过滤、字段精确取值都在这里）

### 主进程接线 / 抓包

- `src/main/main.js`
- `src/main/register-api-ipc.js`
- `src/main/api-traffic-recorder.js`
- `src/main/api-traffic-sanitizer.js`
- `src/main/shop-manager.js`

### 渲染层 / 文档

- `src/renderer/chat-api-module.js`
- `src/renderer/index.html`
- `docs/pdd-stability-handover.md`
- `docs/chat-api-stability-handover.md`
- `docs/chat-api-recovery-handover.md`
